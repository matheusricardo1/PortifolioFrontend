import { ServerImagePipe } from './server-image.pipe';

describe('ServerImagePipe', () => {
  it('create an instance', () => {
    const pipe = new ServerImagePipe();
    expect(pipe).toBeTruthy();
  });
});
